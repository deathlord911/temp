#!/bin/bash
# Generated by Ansible

LXC_TEMPLATE_STORAGE="{{ zmb_template_storage }}"
LXC_ROOTFS_SIZE="{{ zmb_rootfs_size_gb }}"
LXC_ROOTFS_STORAGE="{{ zmb_rootfs_storage }}"
LXC_SHAREFS_SIZE="{{ zmb_sharefs_size_gb }}"
LXC_SHAREFS_STORAGE="{{ zmb_sharefs_storage }}"

LXC_THREADS={{ zmb_threads }}
LXC_MEM={{ zmb_mem_mb }}
LXC_SWAP={{ zmb_swap_mb }}

LXC_HOSTNAME="${service}"
LXC_DOMAIN="{{ zmb_domain_lower }}"

LXC_DHCP=false
LXC_IP="{{ zmb_ad_join_ip }}/24"
LXC_GW="{{ zmb_gw }}"
# Beim Joiner muss DNS auf den ersten DC zeigen
LXC_DNS="{{ zmb_ad_ip }}"

LXC_BRIDGE="{{ zmb_net_bridge }}"
LXC_VLAN="{{ zmb_net_vlan }}"

LXC_PWD='Start123'
LXC_AUTHORIZED_KEY=~/.ssh/authorized_keys
LXC_TOOLSET="vim htop net-tools dnsutils sysstat mc"
LXC_TIMEZONE="Europe/Berlin"
LXC_LOCALE="de_DE.UTF-8"
LXC_VIM_BG_DARK=1
LXC_RANDOMPWD=32

LXC_RESSOURCE_POOL=""
LXC_AUTOTAG=1
LXC_TAGS="linux,debian,${service}"

ZMB_REALM="{{ zmb_domain_upper }}"
ZMB_DOMAIN="AMAZONISTAN"
ZMB_ADMIN_USER="{{ zmb_admin_user }}"
ZMB_ADMIN_PASS='{{ zmb_admin_pass }}'
ZMB_DOMAIN_ADMINS="domain admins"
ZMB_SHARE="share"
